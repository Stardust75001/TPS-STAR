permissions:
  contents: read
  actions: write
  workflows: write

# --- 🧾 Resume Metrics + Trigger PDF Build ---
name: 🧾 Resume Metrics + Trigger PDF Build

on:
  workflow_dispatch:

jobs:
  resume:
    name: 🧩 Resume Metrics Results
    runs-on: ubuntu-latest
    steps:
      - name: 🧠 Summary
        run: |
          echo "📊 Summary ready — triggering PDF generation..."

      - name: 📤 Trigger PDF Workflow
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: "🧾 Metrics PDF Report"
          ref: DEV
