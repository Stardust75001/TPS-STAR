on:
  workflow_dispatch:
permissions:
  contents: read
  actions: write
  workflows: write

on:
  workflow_dispatch:
permissions:
  contents: read
  actions: write
  workflows: write

permissions:
  contents: read
  actions: write
  workflows: write

# --- 🧾 Resume Metrics + Trigger PDF Build ---
name: 🧾 Resume Metrics + Trigger PDF Build

on:
  workflow_dispatch:

jobs:
  resume:
    name: 🧩 Resume Metrics Results
    runs-on: ubuntu-latest
    steps:
      - name: 🧠 Summary
        run: |
          echo "📊 Summary ready — triggering PDF generation..."

      - name: 📤 Trigger PDF Workflow
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: "🧾 Metrics PDF Report"
          ref: DEV


jobs:
  trigger_next:
    name: 🔁 Trigger Metrics PDF Workflow
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Launch metrics-pdf.yml
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: "metrics-pdf.yml"
          ref: DEV
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}


jobs:
  trigger_next:
    name: 🔁 Trigger Metrics PDF Workflow
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Launch metrics-pdf.yml
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: "metrics-pdf.yml"
          ref: DEV
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
