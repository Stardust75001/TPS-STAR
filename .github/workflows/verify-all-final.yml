name: 🧠 Verify + Resume + PDF + Slack
on:
  workflow_dispatch:
  schedule:
    - cron: "0 8 * * 1"  # chaque lundi à 8h UTC
permissions:
  contents: read
  actions: write

jobs:
  sanity-check:
    runs-on: ubuntu-latest
    steps:
      - name: ✅ Sanity Check
        run: echo "Workflow dispatch now working ✅"
