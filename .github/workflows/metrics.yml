permissions:
  contents: read
  actions: write
  workflows: write

# --- 📊 Verify All Metrics Integrations ---
name: 📊 Verify All Metrics Integrations

on:
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * *"

jobs:
  metrics:
    runs-on: ubuntu-latest
    name: 🔧 Check All Metrics Integrations
    steps:
      - uses: actions/checkout@v4
      - name: 🔍 Verify Environment Variables
        run: |
          echo "SENTRY_DSN: \${{ secrets.SENTRY_DSN }}"
          echo "CLOUDFLARE_TOKEN: \${{ secrets.CLOUDFLARE_TOKEN }}"
          echo "AHREFS_API_KEY: \${{ secrets.AHREFS_API_KEY }}"
          echo "GA4_TOKEN: \${{ secrets.GA4_TOKEN }}"
