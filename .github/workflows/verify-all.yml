# --- 🧠 Verify + Resume All Metrics ---
name: 🧠 Verify + Resume All Metrics

on:
  workflow_dispatch:
  schedule:
    - cron: "0 8 * * 1" # chaque lundi à 8h du matin

jobs:
  run-verify:
    name: 🔍 Verify Metrics Connections
    runs-on: ubuntu-latest
    steps:
      - name: 🧩 Trigger Metrics Verification
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: "📊 Verify All Metrics Integrations"
          ref: DEV

  run-resume:
    name: 🧾 Resume Metrics Summary
    needs: run-verify
    runs-on: ubuntu-latest
    steps:
      - name: 🧠 Trigger Resume Workflow
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: "🧾 Resume Metrics + Trigger PDF Build"
          ref: DEV
