name: 📧 Envoi test - Rapport par email

on:
  workflow_dispatch:

jobs:
  send_email:
    name: Envoi d’un email HTML
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout du repo
        uses: actions/checkout@v3

      - name: 📧 Envoi via SMTP (send-mail@v3)
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.SMTP_HOST }}
          server_port: ${{ secrets.SMTP_PORT }}
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          subject: "🚀 Test d’envoi Email TPS-STAR"
          to: ${{ secrets.EMAIL_TO }}
          from: ${{ secrets.EMAIL_FROM }}
          content_type: text/html
          body: |
            <h2 style="font-family:Arial">📊 Rapport TPS</h2>
            <p style="font-family:Arial">Ceci est un test d’envoi d’email automatique depuis GitHub Actions.</p>
            <p>✅ SMTP configuré avec succès !</p>
            <hr>
            <small>Email généré automatiquement par <b>TPS-STAR</b></small>
