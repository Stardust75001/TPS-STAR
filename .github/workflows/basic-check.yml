name: basic-check
on:
  pull_request:
    branches: [ main ]
jobs:
  ok:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Sanity
        run: echo "OK ✅"
